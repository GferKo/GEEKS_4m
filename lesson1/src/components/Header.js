import React from 'react';
import Title from "./Title";

const Header = () => {
    return (
        <div>
            <Title title={'Header'} name={'Saadi'}/>
        </div>
    );
};

export default Header;